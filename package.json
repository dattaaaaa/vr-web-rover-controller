{
  "name": "quest-rover-controller",
  "version": "1.0.0",
  "description": "Control a rover with a Meta Quest 2 via Node.js and MQTT.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "VD",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mqtt": "^5.3.4",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
